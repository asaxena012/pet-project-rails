<h1>Pets#index</h1>
<p>Find me in app/views/pets/index.html.erb</p>

<h1>All pets</h1>

<table>
  <tr>
    <th colspan="5"></th>
  </tr>
  <% @pets.each do |p| %>
    <tr>
        <td><%= link_to p.name, pet_path(p)  %></td>
    </tr>
  <% end %>
</table>
